# Security policy

Do not include message bodies, access tokens, crypto keys, unredacted Matrix identifiers, or database/spool files in reports. Report vulnerabilities privately through GitHub's security advisory interface.

The default configuration is fail closed and starts Matrix in shadow mode. Disabling encryption, device trust, membership checks, or authorization is not recommended.
