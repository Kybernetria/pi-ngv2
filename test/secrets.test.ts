import test from "node:test";import assert from "node:assert/strict";import {chmodSync,mkdtempSync,rmSync,writeFileSync} from "node:fs";import {tmpdir} from "node:os";import {join} from "node:path";import {readSecretFile,redactSecret} from "../src/secrets.ts";
test("secret files require strict permissions and redact recursively",()=>{const dir=mkdtempSync(join(tmpdir(),"pi-ngv2-secret-")),file=join(dir,"token");try{writeFileSync(file,"abc",{mode:0o644});assert.throws(()=>readSecretFile(file,"token"),/0600/);chmodSync(file,0o600);assert.equal(readSecretFile(file,"token"),"abc");assert.deepEqual(redactSecret({accessToken:"x",nested:{password:"y",ok:1}}),{accessToken:"[REDACTED]",nested:{password:"[REDACTED]",ok:1}});}finally{rmSync(dir,{recursive:true,force:true});}});
